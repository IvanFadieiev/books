<% if book.draft == false %>
  <%= render partial: "book_list", locals: { book: book } %>
<% elsif book.draft && current_user.id == book.user.id %>
  <%= render partial: "book_list", locals: { book: book } %>
<% end %>
